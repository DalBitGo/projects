# StoreBridge 개발 로드맵

## 📍 현재 위치: Phase 1-B (백엔드 구현 70% 완료)

---

## 🗺️ 전체 개발 단계

```
Phase 0: 기획 & 설계 (✅ 완료)
    ↓
Phase 1: 백엔드 코어 구현 (🟡 70% 진행 중)
    ↓
Phase 2: 외부 API 연동 (❌ 대기)
    ↓
Phase 3: 테스트 & 안정화 (❌ 대기)
    ↓
Phase 4: 프론트엔드 (❌ 대기 - 선택 사항)
    ↓
Phase 5: 배포 & 운영 (❌ 대기)
```

---

## Phase 0: 기획 & 설계 ✅ (100% 완료)

**목표**: 프로젝트 전체 구조 설계

**완료된 것**:
- [x] 프로젝트 요구사항 정의 (PRD)
- [x] 시스템 아키텍처 설계 (ARCHITECTURE.md - 53KB)
- [x] 데이터베이스 스키마 설계 (DATABASE_SCHEMA.md - 32KB)
- [x] API 명세서 작성 (API_SPECIFICATION.md - 21KB)
- [x] 테스트 계획 수립 (TEST_PLAN.md - 53KB)
- [x] 배포 전략 수립 (DEPLOYMENT_GUIDE.md - 24KB)

**결과물**: 273KB 문서 (9개 파일)

---

## Phase 1: 백엔드 코어 구현 🟡 (70% 진행 중)

**목표**: 외부 API 없이 동작하는 백엔드 시스템 구현

### Phase 1-A: 인프라 & 기본 구조 ✅ (100%)

**완료된 것**:
- [x] 프로젝트 구조 생성
- [x] Docker 환경 설정 (PostgreSQL, Redis)
- [x] FastAPI 서버 기본 설정
- [x] 데이터베이스 연결 (SQLAlchemy Async)
- [x] 테이블 생성 (4 테이블, 11 인덱스)
- [x] 환경 설정 (.env, config.py)

**파일**:
```
app/
├── main.py            # FastAPI 앱
├── config.py          # 설정
├── database.py        # DB 연결
└── models/
    ├── base.py
    └── product.py     # 4개 모델
```

---

### Phase 1-B: 비즈니스 로직 구현 🟡 (80%)

**완료된 것**:
- [x] Rate Limiter (Redis + Lua script) - 94% 테스트 커버리지
- [x] Option Mapper (1D/2D/3D 옵션 파싱) - 96% 커버리지
- [x] Validators (금칙어, 상품 검증)
- [x] Job API (CRUD 엔드포인트)
- [x] Celery Worker 구현 (import/register 작업)

**파일**:
```
app/
├── services/
│   ├── rate_limiter.py      ✅ (49줄, 14 테스트)
│   └── option_mapper.py     ✅ (77줄, 16 테스트)
├── validators/
│   ├── forbidden_word_validator.py  ✅
│   └── product_validator.py         ✅
├── workers/
│   ├── celery_app.py        ✅ (36줄)
│   └── tasks.py             ✅ (408줄)
└── api/
    └── jobs.py              ✅ (294줄)
```

**현재 상태**:
- ✅ 모든 코드 작성 완료
- ✅ FastAPI 서버 실행 가능
- ✅ PostgreSQL/Redis 연동 완료
- ⚠️ **외부 API(도매꾹, 네이버)는 목(mock) 클라이언트만 구현**

---

### Phase 1-C: API 클라이언트 스켈레톤 ✅ (100% - 목 구현만)

**완료된 것**:
- [x] 도매꾹 API 클라이언트 (인터페이스만)
- [x] 네이버 API 클라이언트 (인터페이스만)

**파일**:
```
app/
└── connectors/
    ├── domeggook_client.py  ✅ (83줄) - 목 응답만 반환
    └── naver_client.py      ✅ (100줄) - 목 응답만 반환
```

**현재 동작**:
```python
# 현재는 이렇게 동작
async with DomeggookClient() as client:
    response = await client.get_item_list()
    # 실제 API 호출 없음
    # 하드코딩된 목 데이터 반환: {"items": [...]}
```

**아직 안 된 것**:
- ❌ 실제 API 키로 호출
- ❌ 실제 상품 데이터 가져오기
- ❌ 에러 핸들링 (429 Rate Limit 등)

---

### Phase 1-D: 미완성 기능 ❌ (0%)

**아직 구현 안 된 것**:
- [ ] 이미지 처리 파이프라인
  - `app/services/image_processor.py` (없음)
  - 이미지 다운로드, 리사이즈, WebP 변환, S3 업로드

- [ ] 카테고리 매핑
  - `app/services/category_mapper.py` (없음)
  - 도매꾹 카테고리 → 네이버 카테고리 변환

---

## Phase 2: 외부 API 연동 ❌ (0% - **다음 단계**)

**목표**: 실제 도매꾹/네이버 API 연동

### 필요한 것:

#### 1. API 키 발급 (소요: 1-3일)
- [ ] **도매꾹 API 키** 발급
  - 사이트: https://openapi.domeggook.com
  - 필요: 사업자 등록 (개인도 가능)
  - Rate Limit: 180 calls/min, 15,000/day

- [ ] **네이버 Commerce API 키** 발급
  - 사이트: https://commerce.naver.com
  - 필요: 스마트스토어 개설 + 판매자 센터 승인
  - Rate Limit: **2 TPS** (매우 엄격!)
  - IP 화이트리스트 등록 필요

#### 2. API 클라이언트 실제 구현 (소요: 8시간)
```python
# app/connectors/domeggook_client.py 수정

async def get_item_list(self, ...):
    # 기존: 하드코딩된 목 데이터 반환
    # return {"items": [{"item_id": "MOCK-001", ...}]}

    # 변경: 실제 API 호출
    response = await self._client.get(
        "/getItemList",
        params={
            "key": self.api_key,  # 실제 API 키 사용
            "page": page,
            ...
        }
    )
    return self._decode_response(response)  # EUC-KR 인코딩 처리
```

**작업 항목**:
- [ ] 도매꾹 API 실제 호출 구현
- [ ] 네이버 API 실제 호출 구현
- [ ] 에러 핸들링 (429, 401, 500 등)
- [ ] Rate Limit 준수 확인
- [ ] OAuth 토큰 갱신 로직

#### 3. Integration 테스트 (소요: 6시간)
- [ ] VCR.py로 API 응답 레코딩
- [ ] 실제 API 호출 테스트 (Sandbox 환경)
- [ ] Rate Limit 테스트
- [ ] 에러 시나리오 테스트

**완료 조건**:
- ✅ 실제 도매꾹에서 상품 리스트 가져오기 성공
- ✅ 실제 네이버에 상품 1개 등록 성공
- ✅ Rate Limiter가 2 TPS 준수 확인

---

## Phase 3: 테스트 & 안정화 ❌ (0%)

**목표**: 프로덕션 배포 가능한 수준의 안정성 확보

### 작업 항목:

#### 1. 실패한 유닛 테스트 수정 (소요: 35분)
- [ ] Option Mapper 공백 처리
- [ ] Separator 일관성 검증
- [ ] Rate Limiter 예외 타입

#### 2. E2E 테스트 (소요: 8시간)
```python
# tests/e2e/test_complete_flow.py

async def test_import_10_products():
    """도매꾹에서 10개 상품 가져와서 네이버에 등록"""
    # 1. Job 생성
    response = await client.post("/v1/jobs", json={...})
    job_id = response.json()["data"]["job_id"]

    # 2. Celery Worker가 처리 대기 (최대 5분)
    await wait_for_job_completion(job_id, timeout=300)

    # 3. 결과 확인
    job = await get_job(job_id)
    assert job.status == "COMPLETED"
    assert job.success_count >= 8  # 80% 성공률
```

#### 3. 성능 테스트 (소요: 4시간)
- [ ] Locust로 부하 테스트
- [ ] Rate Limiter 정확도 테스트 (0% 위반)
- [ ] 5,000 products/day 처리 가능 확인

---

## Phase 4: 프론트엔드 ❌ (선택 사항)

**현재 상태**: 프론트엔드 없음 (REST API만 존재)

### 옵션 1: API만 제공 (추천)
**장점**:
- ✅ 개발 시간 단축
- ✅ Swagger UI로 충분히 테스트 가능
- ✅ 다른 시스템과 통합 용이

**사용 방법**:
```bash
# Swagger UI
http://localhost:8000/docs

# cURL로 직접 호출
curl -X POST http://localhost:8000/v1/jobs -d '{...}'
```

---

### 옵션 2: 관리자 대시보드 제작 (소요: 40시간)

**필요한 기능**:
1. Job 생성 폼
2. Job 상태 모니터링 (실시간)
3. 상품 목록 조회
4. 수동 검토 큐 (Manual Review)
5. 로그 뷰어

**기술 스택 선택**:

#### A. React + TypeScript (추천)
```
frontend/
├── src/
│   ├── components/
│   │   ├── JobList.tsx
│   │   ├── JobDetail.tsx
│   │   ├── ProductList.tsx
│   │   └── ManualReviewQueue.tsx
│   ├── api/
│   │   └── client.ts        # Axios API 클라이언트
│   └── App.tsx
├── package.json
└── vite.config.ts
```

**작업 예상**:
- Day 1-2: 프로젝트 설정 + API 클라이언트
- Day 3-4: Job 관리 화면
- Day 5: 상품 목록 화면
- Day 6: 수동 검토 화면
- Day 7: 실시간 업데이트 (WebSocket)

#### B. Streamlit (빠른 프로토타입)
```python
# streamlit_app.py

import streamlit as st
import requests

st.title("StoreBridge 관리자")

# Job 생성
with st.form("create_job"):
    limit = st.number_input("상품 개수", 1, 100, 10)
    if st.form_submit_button("Import 시작"):
        response = requests.post(
            "http://localhost:8000/v1/jobs",
            json={"type": "IMPORT", "config": {"limit": limit}}
        )
        st.success(f"Job 생성됨: {response.json()}")

# Job 목록
jobs = requests.get("http://localhost:8000/v1/jobs").json()
st.dataframe(jobs["data"]["items"])
```

**소요 시간**: 4시간 (프로토타입)

---

### 옵션 3: 프론트엔드 없이 운영 (추천) ⭐

**대신 사용하는 것**:
1. **Swagger UI** (`/docs`) - API 테스트
2. **PostgreSQL Admin** (pgAdmin, DBeaver) - 데이터 조회
3. **Redis Commander** - Redis 상태 모니터링
4. **Grafana** - 메트릭 대시보드
5. **Celery Flower** - Celery 모니터링

```bash
# Celery Flower 실행 (웹 UI)
celery -A app.workers.celery_app flower --port=5555
# http://localhost:5555
```

---

## Phase 5: 배포 & 운영 ❌ (0%)

### 작업 항목:

#### 1. Docker 이미지 빌드 (소요: 2시간)
```dockerfile
# Dockerfile
FROM python:3.11-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install -r requirements.txt
COPY . .
CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]
```

#### 2. CI/CD 파이프라인 (소요: 4시간)
```yaml
# .github/workflows/ci-cd.yml
name: CI/CD
on: [push, pull_request]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Run tests
        run: pytest tests/
  deploy:
    needs: test
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to production
        run: kubectl apply -f k8s/
```

#### 3. 모니터링 설정 (소요: 6시간)
- [ ] Prometheus 메트릭 수집
- [ ] Grafana 대시보드
- [ ] Sentry 에러 트래킹
- [ ] Slack 알림

---

## 🎯 추천 개발 순서

### 지금 바로 할 수 있는 것 (API 키 없이):

#### 1주차: 백엔드 안정화
- Day 1: 실패한 테스트 3개 수정
- Day 2: E2E 테스트 작성 (목 데이터 사용)
- Day 3: Celery Worker 실제 동작 테스트
- Day 4-5: 이미지 처리 파이프라인 구현

#### 2주차: API 연동 준비
- Day 1-2: 도매꾹 API 키 발급 대기
- Day 3: 도매꾹 API 클라이언트 실제 구현
- Day 4-5: 네이버 API 키 발급 대기

#### 3주차: 실제 연동
- Day 1: 네이버 API 클라이언트 실제 구현
- Day 2-3: Integration 테스트
- Day 4-5: 실제 상품 10개 등록 테스트

#### 4주차: 프론트엔드 (선택)
- Option A: Streamlit 프로토타입 (1일)
- Option B: React 대시보드 (5일)
- Option C: 프론트엔드 스킵, 운영 도구 설정 (2일)

---

## 📊 현재 완성도

| 구성 요소 | 완성도 | 비고 |
|-----------|--------|------|
| 문서화 | 100% | 273KB, 프로덕션 수준 |
| 데이터베이스 | 100% | 4 테이블, 운영 가능 |
| FastAPI 서버 | 90% | 실행 가능, API 키만 추가하면 됨 |
| Celery Worker | 100% | 구현 완료, 테스트 필요 |
| Rate Limiter | 100% | 프로덕션 준비 완료 |
| Option Mapper | 95% | 3개 테스트 실패 |
| 도매꾹 API | 30% | 인터페이스만, 실제 호출 미구현 |
| 네이버 API | 30% | 인터페이스만, 실제 호출 미구현 |
| 이미지 처리 | 0% | 미구현 |
| 카테고리 매핑 | 0% | 미구현 |
| 프론트엔드 | 0% | 미구현 (선택) |
| 배포 | 0% | 미구현 |

**종합 완성도: 55%**

---

## 🚀 빠른 시작 (API 키 없이 테스트)

```bash
# 1. 서버 실행
uvicorn app.main:app --reload

# 2. Celery Worker 실행 (별도 터미널)
celery -A app.workers.celery_app worker --loglevel=info

# 3. Job 생성 (목 데이터로 테스트)
curl -X POST http://localhost:8000/v1/jobs \
  -H "Content-Type: application/json" \
  -d '{
    "type": "IMPORT",
    "config": {
      "source": "domeggook",
      "limit": 5,
      "auto_register": true
    }
  }'

# 4. Swagger UI에서 결과 확인
# http://localhost:8000/docs
```

---

## 📝 다음 작업 선택지

### A. API 키 발급 후 실제 연동 (추천) ⭐
- 소요: 1-3일 (발급 대기) + 1일 (개발)
- **실제 동작하는 시스템 완성**

### B. 백엔드 안정화 (API 키 없이 가능)
- 소요: 3-5일
- 테스트 커버리지 100%, E2E 테스트

### C. 프론트엔드 개발
- 소요: 1일 (Streamlit) 또는 5일 (React)
- 사용자 친화적 UI

### D. 배포 준비
- 소요: 2-3일
- Docker, CI/CD, 모니터링

**어떤 것을 먼저 하시겠습니까?**

---

**작성일**: 2025-10-17
**버전**: 1.0
**프로젝트**: StoreBridge
